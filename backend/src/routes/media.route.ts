import express from "express";

export const mediaRouter = express.Router();

// upload image in tweet
mediaRouter.post("tweet", (req, res) => {});

// uploda profile pic
mediaRouter.post("profile-pic", (req, res) => {});
